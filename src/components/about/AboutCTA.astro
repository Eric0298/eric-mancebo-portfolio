---
import { getPagePath, type Language, type TranslationDictionary } from "../../i18n/config";
import ButtonLink from "../ui/ButtonLink.astro";
import Container from "../ui/Container.astro";

interface Props {
  language: Language;
  translations: TranslationDictionary;
}

const { language, translations } = Astro.props;
const cta = translations.aboutPage.cta;
const projectsPath = getPagePath(language, "projects");
const contactPath = getPagePath(language, "contact");
---

<section class="about-cta" aria-label={cta.summary}>
  <Container size="wide" class="about-cta__inner" data-motion="reveal">
    <div class="about-cta__poster">
      <div class="about-cta__copy">
        <p class="about-cta__message">{cta.summary}</p>
        <div class="about-cta__actions">
          <ButtonLink href={projectsPath}>{cta.projectsLabel}</ButtonLink>
          <ButtonLink href={contactPath} variant="secondary">{cta.contactLabel}</ButtonLink>
        </div>
      </div>

      <div class="about-cta__footer-strip">
        <p>{translations.metadata.role}</p>
        <nav class="about-cta__nav" aria-label={translations.navigation.footerLabel}>
          {translations.navigation.items.map((item) => (
            <a href={item.href}>{item.label}</a>
          ))}
        </nav>
      </div>

      <p class="about-cta__wordmark" aria-hidden="true">{cta.brandWordmark}</p>
    </div>
  </Container>
</section>
