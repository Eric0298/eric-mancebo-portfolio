---
import { getPagePath, type Language } from "../../i18n/config";
import { useTranslations } from "../../i18n/useTranslations";
import BaseLayout from "../../layouts/BaseLayout.astro";
import AutomationFlowSection from "../sections/AutomationFlowSection.astro";
import PageHero from "../sections/PageHero.astro";

interface Props {
  language: Language;
}

const { language } = Astro.props;
const translations = useTranslations(language);
const canonicalPath = getPagePath(language, "automation");
const page = translations.pages.automation;
---

<BaseLayout
  language={language}
  translations={translations}
  canonicalPath={canonicalPath}
  title={page.metaTitle}
  description={page.metaDescription}
>
  <PageHero page={page} pageKey="automation" />
  <AutomationFlowSection translations={translations} />
</BaseLayout>
